# Copy default Stages into each Project

The Workflow Template is only a set of suggestions copied into a new Project, after which that Project owns its Stage list. PMO Officers may add, remove, rename, or reorder upcoming Stages without affecting other Projects; the model deliberately avoids skipped or not-applicable states, while completed transition history remains immutable so Project-level flexibility does not rewrite the past.
